# Cloudmesh AI OpenAPI Configuration

llm:
  endpoint: "http://localhost:8080/v1"
  model: "gpt-4"
  api_key: "your-api-key-here"
  timeout: 30
  temperature: 0.7

generation:
  use_llm: true
  prompt_template: "Generate an OpenAPI 3.0 specification for the following Python code..."