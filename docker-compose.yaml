version: '3.8'

services:
  ai-openapi-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SERVER_SCRIPT=my_ai_server_cmsoaserver.py
      - PORT=8080
    volumes:
      - .:/app
    restart: always